#include<bits/stdc++.h>
#define loop(i, a, b) for(int i=a;i<b;i++)
#define ll long long
using namespace std;
int dp[20][20][20]
int main()
{
    ios_base::sync_with_stdio(0);
    int t, n[3];
    cin>>t;
    while(t--)
    {
        cin>>n[0]>>n[1]>>n[2];
        sort(n, n+3);

    }

    return 0;
}
