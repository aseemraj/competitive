#include<bits/stdc++.h>
#define loop(i, a, b)  for(int i=a;i<b;i++)
typedef long long ll;
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    int n;
    string st;
    cin>>n;
    int p[n];
    loop(i, 0, n){cin>>p[i];}
    loop(i, 0, n)
    {
        cin>>st;
        loop(j, 0, n)
        {
            a[i][j] = st[i]=='1'?1:0;
        }
    }
    loop(i, 0, n)
    {
        loop(j, 0, n)
        {

        }
    }
    return 0;
}
