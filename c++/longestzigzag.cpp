#include <iostream>
#include <cstdlib>
#include <vector>
#include <list>
#include <map>
#include <set>
#include <queue>
#include <deque>
#include <stack>
#include <bitset>
#include <algorithm>
#include <functional>
#include <numeric>
#include <utility>
#include <sstream>
#include <iomanip>
#include <cstdio>
#include <cmath>
#include <ctime>
#define all(c) c.begin(), c.end()
#define range(i, n) for(i=0;i<n;i++)

using namespace std;

int main()
{
    int t, n, i, j;
    cout<<"Test cases: ";
    cin>>t;
    while(t--)
    {
        cout<<"Number of numbers: ";
        cin>>n;
        int a[n];
        pair <int, bool> s[n];
        range(i, n)
        {
            cin>>a[i];
            s[i].first=1;
            s[i]
        }
        range(i, n)
        {
            for(j=i-1;j>=0;j--)
            {
                if()
            }
        }
    }
    return 0;
}
